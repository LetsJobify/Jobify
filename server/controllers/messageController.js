const db = require('../database/msgModel');

const messageController = {};

// get Messages
messageController.getMessages = (req, res, next) => {};

// post message
messageController.getMessage= (req, res, next) => {};

module.exports = messageController;
